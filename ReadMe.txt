步骤：
1. 确定url转换成文件地址的方案
2. 如何实现控制页面定点显示到第几行
3. 在页面上逐行转换代码文本为需要的格式（需要有行号，后续考虑代码的关键词高亮等功能）
4. 页面共享功能：
 a. 把当前输入的文件地址保存，从"/"进入的直接读取当前地址，而输入有效文件名的，则可以修改当前的文件路径。
 b. 发送自己的行号给他人，这是双向的。可以通过websocket实现。对方收到后，直接跳转到指定行号。
